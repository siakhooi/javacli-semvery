#!/bin/sh
set -e

RELEASE_VERSION=1.1.0
RELEASE_TITLE="${RELEASE_VERSION}"
RELEASE_NOTE="- add -o/--output json/table
- add -s/--silent
- tabular output with variable length"

export RELEASE_VERSION RELEASE_TITLE RELEASE_NOTE
